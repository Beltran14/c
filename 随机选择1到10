#include <stdio.h>  
#include <stdlib.h>  
#include <time.h>  
  
int main() {  
    int num;  
    srand(time(NULL)); // 使用当前时间作为随机数生成器的种子  
    num = rand() % 10 + 1; // 生成1到10之间的随机数  
    printf("随机选择的自然数是：%d\n", num);  
    return 0;  
}
